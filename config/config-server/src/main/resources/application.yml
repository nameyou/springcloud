server:
  port: 8000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nameyou/spriingconfig-11.git
          force-pull: true # 强制拉取配置文件
#          search-paths: 路径
#          username:
#          password: